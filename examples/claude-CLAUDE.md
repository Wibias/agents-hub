# Claude Code — pointer to agent hub

Canonical identity and ops live in the agent hub. Do not duplicate them here.

Skills live under `~/.agents/skills/` (not a second catalog under `~/.claude/skills/`).

@~/.agents/SOUL.md
@~/.agents/AGENTS.md
@~/.agents/CONTEXT.md
